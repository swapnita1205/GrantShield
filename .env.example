# From Supabase dashboard → Settings → API → Project URL
SUPABASE_URL= "https://occaadkdboosqzrisbnj.supabase.co"
# From Supabase dashboard → Settings → API → service_role key (secret)
SUPABASE_SERVICE_ROLE_KEY= "sb_publishable_MqrLR1O0EUBFYlgp231R-A_qEJODkOH"

# Optional — get a free key at app.fac.gov/developers. Without it, FAC audit
# data is skipped (audit_data / audit_findings stay empty).
FAC_API_KEY= " JOuy1F2aJC5K9Wme89Y5IE7DpqSyQegkUOL9Xcba"
# Optional — get from your SAM.gov account profile. Without it, SAM entity
# data is skipped (sam_entities stays empty).
SAM_API_KEY=
# Optional (paid) — Crustdata trial key. Without it, Crustdata lookups are
# skipped (crustdata_profiles stays empty).
CRUSTDATA_API_KEY=
